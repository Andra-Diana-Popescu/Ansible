linux:
 hosts:
  # add IP-1:
  # add IP-2:
 vars:
  ansible_user: ec2-user
  ansible_ssh_private_key_file: /opt/app/Your_DEV_sshkey # Corrected variable name
all:
 hosts:
  # add IP-1:
  # add IP-2:
webserver:
 hosts:
  # add IP-1:
dbserver:
 hosts:
  # add IP-2:
